// import {unid} from 'lodash';
import insane from 'insane';
import jsonp from 'jsonp';
import { apiKey as key, first, third } from './config';
import { createUrl } from './user'

console.log(key, first, third);
console.log(createUrl()); 
